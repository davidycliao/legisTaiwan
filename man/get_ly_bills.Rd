% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ly_bills.R
\encoding{UTF-8}
\name{get_ly_bills}
\alias{get_ly_bills}
\title{Fetch and Parse Legislative Yuan Bills}
\usage{
get_ly_bills(
  page = 1,
  per_page = 20,
  term = NULL,
  session = NULL,
  bill_type = NULL,
  current_status = NULL,
  process_status = NULL,
  proposer = NULL,
  cosigner = NULL,
  source = NULL,
  bill_id = NULL,
  law_id = NULL,
  meeting_code = NULL,
  show_progress = TRUE
)
}
\arguments{
\item{page}{integer. Page number for pagination (default: 1)}

\item{per_page}{integer. Number of items per page (default: 20)}

\item{term}{integer. Legislative term, e.g., 11}

\item{session}{integer. Legislative session period, e.g., 2}

\item{bill_type}{string. Type of bill:
\itemize{
\item "法律案" (Law Bill)
\item "中央政府總預算案" (Central Government Budget Bill)
\item "預(決) 算決議案" (Budget/Final Account Resolution)
\item "定期報告" (Regular Report)
\item "行政命令(層級)" (Administrative Order)
\item "院內單位來文" (Internal Document)
}}

\item{current_status}{string. Current bill status, e.g., "排入院會"}

\item{process_status}{string. Status in process flow, e.g., "排入院會 (交內政委員會)"}

\item{proposer}{string. Bill proposer name}

\item{cosigner}{string. Bill cosigner name}

\item{source}{string. Source of bill, e.g., "委員提案"}

\item{bill_id}{string. Bill ID number}

\item{law_id}{string. Related law ID number}

\item{meeting_code}{string. Meeting code, e.g., "院會-11-2-3"}

\item{show_progress}{logical. Whether to display progress bar (default: TRUE)}
}
\value{
A list containing:
\itemize{
\item metadata - List of pagination info and applied filters
\item bills - Data frame of bill details
}
}
\description{
Retrieves bill information from the Legislative Yuan API with comprehensive filter options.
Supports filtering by term, session, bill type, status, and other attributes.
Returns both metadata and detailed bill information.
}
\examples{
# Get law bills
bills <- get_ly_bills(
  term = 11,
  bill_type = "法律案",
  show_progress = TRUE
)

# Get budget bills
bills <- get_ly_bills(
  term = 11,
  bill_type = "中央政府總預算案",
  show_progress = TRUE
)

# Get administrative orders
bills <- get_ly_bills(
  term = 11,
  bill_type = "行政命令(層級)",
  show_progress = TRUE
)

}
