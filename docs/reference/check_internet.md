# A Check for Internet Connectivity.

A Check for Internet Connectivity.

## Usage

``` r
check_internet(x = curl::has_internet())
```

## Arguments

- x:

  The default value is
  [`curl::has_internet()`](https://jeroen.r-universe.dev/curl/reference/nslookup.html),
  which activate the internet connectivity check.
